{
  "name": "council",
  "version": "0.5.2",
  "private": true,
  "type": "module",
  "description": "Council v5.2 — evidence-graded decision quality with venture clustering, forced red-team challenge, Chief resolution, confidence and falsification tests.",
  "scripts": {
    "start": "node server.mjs",
    "check": "node --check server.mjs && node --check lib/tools.mjs && node --check lib/council.mjs && node --check api/council.mjs && node --check api/status.mjs && node --check api/portfolio.mjs && node --check public/app.js && node --check tests/decision-quality.mjs",
    "test:decision": "node tests/decision-quality.mjs",
    "build": "npm run check"
  },
  "engines": { "node": ">=20" }
}
