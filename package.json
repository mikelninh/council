{
  "name": "council",
  "version": "0.5.1",
  "private": true,
  "type": "module",
  "description": "Council v5.1 — an evidence-first AI company OS with authenticated private GitHub portfolio scans, README inspection, execution traces, and human-controlled action.",
  "scripts": {
    "start": "node server.mjs",
    "check": "node --check server.mjs && node --check lib/tools.mjs && node --check lib/council.mjs && node --check api/council.mjs && node --check api/status.mjs && node --check api/portfolio.mjs && node --check public/app.js",
    "build": "npm run check"
  },
  "engines": { "node": ">=20" }
}
